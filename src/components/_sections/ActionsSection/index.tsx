import AppButton from '../../AppButton';

export default function ActionsSection() {
  return (
    <section className="actions">
      <AppButton text="Login" level="info" />
    </section>
  );
}
